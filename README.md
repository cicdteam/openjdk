# OpenJDK docker images based on Ubuntu:16.04

The same as official OpenJDK docker images but `ubuntu:16.04` used as base image instead of `debian:stretch`

### Versions

- 8-jdk-xenial
- 8-jdk-slim-xenial
- 8-jre-slim-xenial
